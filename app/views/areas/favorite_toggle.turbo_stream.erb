<%# <%= render(partial: 'areas/favorite_toggle', locals: { area_key: area_key, favorited: favorited }) %>
<%# <%= render partial: 'areas/favorites' %>

<%= turbo_stream.replace("favorite-toggle-#{@area_key}", partial: 'areas/favorite_toggle', locals: { area_key: @area_key, favorited: @favorited }) %>
<%= turbo_stream.replace("favorites", partial: 'areas/favorites') %>